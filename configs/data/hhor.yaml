type: HOI
data_dir: ${environment.output}/../HHOR_as_HOI4D/${data.index}
index: 6_AirPods
downscale: 1
noise: 0
scale_radius: 3  # what does this do 
val_rayschunk: 10000
volume_size: 6
len: 1000
offset: 40
ratio: 1

batch_size: 1       # one batch, one image
pin_memory: True
N_rays: 1024        # N_rays for training
surface_downscale: 8  # 1920 / 6 = 320
val_downscale: 30
surface_rayschunk: 1024